{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the desktop shell. The Rust main process starts the sidecar directly.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
