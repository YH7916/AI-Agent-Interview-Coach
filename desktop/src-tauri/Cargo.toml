[package]
name = "ai-agent-interview-coach"
version = "0.1.2"
description = "Local-first AI Agent mock interview coach"
authors = ["YH7916"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = []
